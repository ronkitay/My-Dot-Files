#!/bin/sh

if [[ $# == 1 ]];
then
    cd $1
    export WORKSPACE_NAME="$(basename $(pwd))"
    export NO_STATE_WARNING="y"
    USE_SHARED_HISTORY=y dockerrunit basic-env:$(cat ${HOME}/.my-docker-images.release) -v $(pwd):/root/proj/
else
    USE_SHARED_HISTORY=y dockerrunit basic-env:$(cat ${HOME}/.my-docker-images.release) 
fi

